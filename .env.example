# 任意 OpenAI 兼容服务：填入对应的 base_url 和 key
OPENAI_API_KEY=sk-xxxx
OPENAI_BASE_URL=https://api.deepseek.com/v1
CHAT_MODEL=deepseek-chat
EMBEDDING_MODEL=text-embedding-3-small
# 当前模型服务不支持 embeddings 时，自动使用本地轻量向量兜底。
LOCAL_EMBEDDING_FALLBACK=true

# 可选：用于 tools/web_search.py 的真实网络搜索。未填写时会自动使用 mock 示例结果。
WEB_SEARCH_API_KEY=
# WEB_SEARCH_ENDPOINT=https://api.search.brave.com/res/v1/web/search
